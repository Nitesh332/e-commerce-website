import React from 'react';

export const metadata = {
  title: 'ShopWave — Update (Static)'
};

export default function UpdatePage() {
  return (
    <div style={{width: '100%', height: '100vh'}}>
      <iframe src="/update.html" title="ShopWave Update" style={{border: 'none', width: '100%', height: '100%'}} />
    </div>
  );
}
